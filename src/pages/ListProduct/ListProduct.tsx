import React from 'react';
import styles from './ListProduct.module.scss';

const ListProduct: React.FC = (props) => {
  return (
    <div>ListProduct component</div>
  );
}

export default ListProduct;